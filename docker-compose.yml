version: '3'

networks:
  intelmq-network:
    driver: bridge

services:
  intelmq:
    build: intelmq/
    depends_on:
      - ubuntu
      - redis
    image: intelmq
    restart: "no"
    logging:
      driver: syslog
    networks:
      - intelmq-network

  redis:
    image: redis
    build: redis/
    depends_on:
      - ubuntu
    expose:
     - 6379
    networks:
      - intelmq-network

  ubuntu:
    build: ubuntu/
    image: ubuntu:latest

